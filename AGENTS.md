# AGENTS.md - Spoonjoy Delivery

This repository is Spoonjoy's public cross-client delivery control plane. It owns contracts, authorization, evidence validation, finalization, and cleanup coordination. It does not own Spoonjoy domain behavior or provider credentials.

## Workflow

- Use the installed `work-planner` and `work-doer` skills for substantial work.
- Keep planning and doing documents under `<agent>/tasks/` on an agent-scoped branch in a dedicated worktree.
- The adjacent doing document is the implementation source of truth. Follow lexical document order for each unit's first eligible invocation. A closed result without a declared route derives exactly the next executable heading; a declared exact-successor route overrides that lexical successor. Unit 67 alone may emit successful `stop`, while Unit 13a3, Unit 30, Unit 64a3, and Unit 65r alone may emit fail-closed `planning_revision_required` stop through their declared branches. Same-unit, backward, and blocked-resume routes use the doing document's durable repeatable-invocation lifecycle; a heading is not complete until its terminal cursor/acceptance predicate holds. Use strict test-then-implement-then-coverage sequencing.
- Before Unit 67 emits completed or Unit 13a3/Unit 30/Unit 64a3/Unit 65r emits planning revision, the stop owner must canonicalize cursor-free `StopOutputV1`, compute its output digest, precompute the exact closed invocation bytes and resulting terminal-ledger digest, and then the root coordinating host must fsync/readback the exact private `RootTaskPostStopCleanupCursorV1` in Desk against that digest, stable cleanup-registration ID, and stop reason. Only then may the executor atomically close with those byte-identical precomputed bytes. The stop output cannot contain the cursor digest, terminal-ledger digest, Desk locator, or registration receipt, so the binding is acyclic. A Unit 13a3 planning revision is legal only before source-worktree creation, binds `sourceDisposition="empty_before_source_worktrees"`, proves an empty source-worktree/path-map inventory, and requires no retained-source adoption. Unit 30 instead proves terminal preflight containment, binds `sourceDisposition="retained_not_targeted"` to the exact Unit 13c custody plus its sealed path map, and transfers that unchanged custody through the retained-source adoption lifecycle. Unit 64a3 and Unit 65r likewise require their exact retained-source adoption. After the execution stop, the host must observe executor exit or atomically relinquish the same-process executor role, relocate outside the target worktree, acquire the cleanup lease, and consume one cursor action at a time. Session start/resumption must finish the cursor, prove terminal-ledger bytes unchanged, and produce its terminal receipt before final human completion or a replacement planner cycle. The cursor may remove only its manifest-bound current local delivery worktree/branch and private cleanup state; it grants no source/provider read or mutation authority.
- Reviewer convergence requires two consecutive fresh no-context scrutiny passes over one exact immutable commit/tree. Slot 1 is exactly Tinfoil Hat with the root-bound authority/liveness/recovery/transition lens; only its exact clean terminal opens slot 2. Slot 2 is exactly Stranger With Candy with the root-bound usability/operability/evidence/cleanup lens. Both slots bind zero prior conversation items, exact prompt/lens digests, distinct task identities, and the same head/tree. Every `ExactHeadReviewCursorV1` verdict is exactly `CLEAN` or one finding whose severity is the closed `BLOCKER | MAJOR | MINOR | NIT` union; every non-clean severity enters exactly one context-correct repair successor and only literal `CLEAN` may advance. Any finding, unknown/erased severity, wrong identity/order/lens/context, or reviewed-tree change resets the sequence. In the post-publication repair lifecycle, a reviewer finding also terminally supersedes the current repair cursor and creates exactly one same-unit child cursor at `findingSequence+1` over the inherited branch/worktree custody and current head/tree; no sibling, stale receipt, or clean-forward edge survives.
- Every blocked `suspend` edge uses `ExecutionWakeSubscriptionV1`, `ExecutionWakeNoticeV1`, and one-use `ExecutionWakeReceiptV1`, never a caller-supplied digest. Before the authoritative baseline that may block, the unit arms and readback-verifies the root registry entry for its exact unit, predicate, observer/outbox producer, closed authentication proof kind, exact verifier/key-or-transaction binding, monotonic event domain, and fresh-query adapter. The only proof kinds are atomic transaction receipt, detached signature, keyed MAC, root-ledger observation receipt, claimed monotonic alarm receipt, or platform-inventory event receipt; no testimonial or caller assertion is a proof. The broker durably retains a raced authenticated notice with before/after state, immutable source receipt, retained evidence locator/digest, and monotonic identity. Receipt consumption retires and empties the old subscription/queue/outbox and opens only a same-unit query invocation; that invocation freshly requeries authority before any mutation and either proves the predicate or re-arms and suspends. Every specialized wake subscription, alarm, or capability checkpoint is a strict profile of this envelope. Raw/caller digest, ABA/replayed event, producer or verifier substitution, polling, receipt-only predicate acceptance, mutation-before-requery, or terminal residue fails closed.
- Do not start implementation while the doing document is `NEEDS_REVIEW`.

## Ownership Boundaries

- Before a protected owner-release handoff validates, this delivery task may change only this delivery repository and explicitly authorized inert delivery-control-plane resources.
- The retained source owner may, under its still-exclusive authority, execute only the root-authorized `source-guardian-cutover-v1`, the disjoint `source-protection-observer-v1`, byte-identical receiver-ack merges through protected web/native mains, and the stage-tagged `source-owner-provider-evidence-supersession-v1` required to complete that handoff. The Observer operation is limited to installing the exact predeclared App on web/native with only `metadata:read`, `administration:read`, `contents:read`, `checks:read`, `pull_requests:read`, and `actions:read`; generating/importing its non-exportable key into the source-owned read-only broker; exposing only exact no-redirect REST GET routes plus one fixed read-only GraphQL query digest used by Unit 13a3 for refs/commits/trees/blobs/check runs/protection/rulesets, the retained-owner successor pull request and reviews, protected-tree workflow inventory, repository-wide active-run inventories, and exhaustive repository-wide open-PR/auto-merge/merge-queue inventory; and proving zero write, GraphQL mutation, bypass, general proxy, redirect, undeclared pagination, or credential-export route. Under `X-GitHub-Api-Version: 2026-03-10`, `GitHubActionsActiveStatusV1` is exactly the ordered set `in_progress | queued | requested | waiting | pending`; each status query is one declared page, and an unknown member or continuation fails closed. The fixed GraphQL query enumerates every open PR and its `autoMergeRequest`, `isInMergeQueue`, and `mergeQueueEntry`; `totalCount`, node count, and `pageInfo.hasNextPage=false` must agree, and ownership transfer requires zero auto-merge or queue entry. Evidence supersession is legal only after Unit 13a3 emits exact `ProviderEvidenceSupersessionRequiredV1(stage=pre_ack | post_ack)` for a definitively invalid immutable envelope with zero ownership transfer. Before its first current-evidence query, the retained owner must arm and read back one request-bound `SourceOwnerProviderEvidenceWakeSubscriptionV1`; only the root-bound `SourceOwnerProviderEvidenceTransactionalOutboxV1` may enqueue a changed-state event, including one raced with the baseline. That outbox authenticates immutable provider-lifecycle or retained-source transaction-origin evidence, appends exactly one source-owned transaction receipt, and is the sole wake producer; the request pre-binds its policy, authority-descriptor, and retained-source operation-registry-entry digests before any query. The retained owner may then query current retained-owner provider state, sign one new immutable provider-evidence object, append one predecessor-bound outbound generation at a distinct protected source-owned release-ledger path, and send its immutable commit/path/SHA-256 locator through a stable one-action cursor. The current-evidence query classifies exactly `ready | not_ready | uncertain`; only `ready` may advance to signing. `not_ready` or `uncertain` fsyncs a non-runnable same-cursor suspension; one authenticated changed-state event yields one one-use wake receipt and starts a fresh stable-ID query at `queryAttempt+1`. It never polls, accepts a raw/caller wake, or appends an unusable generation, and every terminal path retires the subscription and proves its queue/outbox empty. `pre_ack` additionally requires authoritative zero receiver-ack cursor/append/record-write/commit/push/PR/review/merge/message effect; `post_ack` preserves every old acknowledgment/event/copy as immutable non-transfer audit evidence. This exact request plus terminal ready cursor is the sole post-freeze route to a new outbound generation; no generic changed-generation receiver-ack route exists. It cannot change product code, deploy/provider/TestFlight state, build, notification, cleanup, old acknowledgment/outbound bytes, or any source main except the later byte-identical acknowledgment copies for that new generation. Those retained-owner actions are handoff inputs, not authority granted to this task, and unrelated source mutation remains forbidden.
- Provider-evidence `ready` ordering is exact and cannot be deferred: the current armed subscription is retired and its queue/outbox are read back empty before signature, object write, commit, or push. A wake receipt retires only the old subscription; the fresh adjacent subscription armed for `queryAttempt+1` must itself retire after that query returns `ready` and before signing. After push, only an absence-only zero-residue audit may run before coordination send; it cannot retire, recreate, or mutate a subscription. Sign-before-retirement, post-push retirement, or terminal residue fails closed.
- Handoff-coordination producer policy is exact and purpose-partitioned before every baseline. The closed `HandoffCoordinationWakePurposeV1` map is exactly `candidate_absent_or_incomplete -> candidate_interop.absent_or_incomplete.source_release_outbox`, `candidate_present_invalid -> candidate_interop.present_invalid.source_release_outbox`, `generation_absent_or_incomplete -> generation_ingestion.absent_or_incomplete.source_release_outbox`, `generation_transient_copy_uncertain -> generation_ingestion.transient_copy_uncertain.source_release_outbox`, `pre_ack_incomplete_or_uncertain -> pre_ack_locator.incomplete_or_uncertain.source_release_outbox`, `pre_ack_provider_supersession_ready -> pre_ack_locator.provider_supersession_ready.source_release_outbox`, `post_ack_delivery_projection_completion -> post_ack_locator.delivery_projection_completion.delivery_ack_projection_outbox`, `post_ack_source_ack_merge_completion -> post_ack_locator.source_ack_merge_completion.source_ack_merge_outbox`, and `post_ack_provider_supersession_ready -> post_ack_locator.provider_supersession_ready.source_release_outbox`. Initial entry purposes are candidate-absent, generation-absent, pre-ack-incomplete, and post-ack-delivery-projection. Unit 13a2a's terminal starts the delivery-projection purpose; a fresh baseline that proves that projection but still lacks source copies may only persist a same-unit nonterminal transition to source-ack-merge purpose before arming again; an exact stage-matching provider-supersession request alone transitions pre-ack or post-ack to its provider-ready source-release purpose. A source-release wake must retire/empty/re-arm, perform a fresh same-unit observation, and route the complete request/cursor pair through ordinary Unit 13a1 ingestion; it never transfers ownership directly.
- Unit 13a0, Unit 13a1, and each Unit 13a3 stage must arm and read back one purpose-bound `HandoffCoordinationWakeSubscriptionV1` before its local coordination baseline. Every subscription binds exactly one registry key and one producer, never a producer union. Only that root-bound transactional-outbox producer may enqueue `HandoffCoordinationChangedNoticeV1`, including one raced with baseline; one matching notice yields one one-use receipt, retires/empties the old subscription, and requires a fresh purpose-bound subscription plus authoritative requery. If a fresh baseline discovers that a different purpose is now required, it retires/empties the current subscription and durably changes purpose in a same-unit nonterminal invocation before the next arm/baseline; it does not block under the old purpose. Raw/caller wake, polling, replay, cross-context or cross-purpose notice, and terminal live subscription/queue/outbox state fail closed.
- The canonical doing document's normative `ExecutionWakeAuthorityCatalogV1` plus deterministic `ExecutionWakeProducerRegistryV1` expansion is the sole wake-authority source and must expand to exactly 153 unique keys: 152 delivery `(unitID,wakePredicateTag)` entries and one retained-source `(operationID,wakePredicateTag)` entry. Phase-multiplexed cursors use only `RootHostExecutionWakeOutboxV1`: each registry row binds one checked-in, root-reviewed `cursorBindingAdapter`, one closed map identity, and one fully expanded `CursorWakeOriginAuthorityDescriptorV1` in the exact 107-member `CursorWakeOriginAuthorityManifestV1`; the manifest fixes every descriptor value, permitted entry kind, registry order, cardinality, and digest before runtime cursor bytes exist. Immutable `CursorWakeAuthorityBindingV1` sidecar bytes then bind one exact cursor revision/entry to that static descriptor, the outbox verifies that origin and atomically records the notice, and the resumed unit's separately listed fresh query remains predicate authority. The retained-source provider-evidence row uses only `SourceOwnerProviderEvidenceTransactionalOutboxV1`; exact-owned unchanged containment retry uses only the root-bound `RenewableContainmentRetryAlarmV1` direct producer. Missing/extra/duplicate/unexpanded rows, opaque or ambiguous map expansion, cardinality/digest drift, derivation from fresh query or caller bytes, missing/cross-revision sidecars, caller/raw origin, or catalog drift fails closed.
- Unit 36 and Unit 60c36 are bridge owners, never installed-queue capability owners. Every exact non-green terminal must atomically create its live/replay pending bridge before any device inventory classification; only Unit 51e, Unit 51j, or Unit 60c51 may execute or suspend the carried queue recovery.
- Unit 60c59b must fsync/read back its lifecycle and batch cursors plus lifecycle and cleanup authorities before any supervisor inventory, namespace, install, adoption, runner, or query effect. It binds every runner/cache to an absence-checked namespace plus immutable OS/filesystem creation identity and performs or reconciles one phase per invocation. Exact-owned unchanged containment failure retires the changed-state subscription and uses one bounded root-bound retry alarm whose fire, retirement, zero-queue proof, and wake receipt are one atomic transaction; exhausted, foreign, ambiguous, residual, or uncertain state remains owned under the authenticated changed-state wake without polling or unmatched mutation. One root-owned `RenewableFreshnessEpisodeV1` carries a cumulative restart counter through batch, activation, convergence, and pre-claim rechecks; exhaustion reaches cleanup-complete failed-iteration ownership. Only after runner/cache/supervisor zero may one successful, failed, or restart-required aggregate CAS atomically retire lifecycle and cleanup authorities. Success alone activates a pass; failure enters Unit 60d1a.
- True-plan-absent finalization keeps three disjoint dormant owners after general setup retirement: cleanup authority, method recovery, and root-precommitted integrity recovery. Method-only drift and every other nonexact authority/key/context/retirement tuple route through the narrow plan-absent recovery cursor; repairable integrity drift restores only exact root-precommitted bytes, while irreparable or uncertain state remains owned and blocked on authenticated changed root/platform evidence. Unit 61b retires all three owners only after exact plan/recovery-WAL readback and one atomic every-target CAS; every no-plan or abort retirement path also accounts for the integrity owner.
- Unit 61d, Unit 61f, Unit 61h, and Unit 61i may route toward pre-claim closure only after they fsync a mandatory `FinalizationRecoveryWakeCursorV1`, dispatch or query its deterministic recovery activation, and read back `FinalizationRecoveryWakeAcceptedV1`. The receiver must atomically journal the request and make recovery runnable before acceptance. Response loss reconciles the same stable episode; absence permits only byte-identical redispatch, mismatch or uncertainty suspends, and Unit 61k rejects a raw, optional, untyped, or unaccepted wake.
- Unit 65r may suspend for external post-publication state only after it derives one closed `PostPublicationExternalWaitPurposeV1` from the immutable ordered recovery-manifest head and arms/readback-verifies the purpose's one registry-key/one-producer `PostPublicationExternalWaitSubscriptionV1` before its authoritative baseline query. A changed sole blocking head requires old-purpose retire/empty proof, durable same-unit `PostPublicationExternalWaitPurposeTransitionV1`, target-purpose arm/readback, and another fresh baseline before suspension. A matching same-purpose notice yields one one-use receipt, retires and empties the old subscription, and permits a fresh same-unit authoritative query only after same-purpose re-arm/readback. Producer unions, simultaneous dependencies/subscriptions, old- or cross-purpose wakes, raw/caller wakes, polling, replay, cross-incident notices, and terminal live subscription/queue/outbox state fail closed.
- This delivery task must not mutate `spoonjoy-v2`, `spoonjoy-apple`, production Cloudflare, App Store Connect, TestFlight, notifications, source worktrees, or source-owned brokers until the complete protected handoff validates. Its sole pre-handoff source access is Unit 13a3's bounded read-only GitHub verification through exact GETs and the one fixed GraphQL query described above; it may not checkout source, use source/provider-release credentials, rerun work, review, merge, write, call a GraphQL mutation, or call any other mutation endpoint.
- Provider operations require the exact claimed authorization, reviewed operation graph, authoritative pre/post state, and terminal receipt defined by the doing document.

## Quality Gates

- Write failing tests before implementation.
- Require 100% statements, branches, functions, and lines for all production logic.
- Treat warnings as failures. Do not suppress warnings or coverage.
- Keep strict TypeScript and fail closed on unknown fields, states, identities, transitions, or provider results.
- Run focused tests during each unit and the full repository gate at convergence units and before every commit.

## Public Evidence

- This repository is public. Never commit secrets, credentials, tokens, personal data, private filesystem paths, raw provider responses, device identifiers, or unredacted account evidence.
- Keep raw transcripts and hostile fixtures in private ephemeral storage until the doing document's sanitization gate permits public summaries and digests.
- Cleanup must use exact run-owned manifests and must not delete pre-existing, dirty, ambiguous, or separately owned state.

## Git

- Keep commits atomic and push each completed logical unit.
- Never rewrite shared history or discard unrelated changes.
- Preserve protected ownership, review, CI, deployment, and cleanup evidence by exact commit and content digest.

## Completion

Notification is a post-completion root-host epilogue, not part of the execution ledger or post-stop cleanup transaction. First require exact `RootTaskPostStopCleanupCompletedV1(stopReason="completed")`, final terminal-ledger equality, zero residue, and no remaining cleanup action. That completed receipt must itself contain typed lowercase 40-hex `deliveryCommit` and canonical public `releaseSetId`, atomically copied from its terminal-ledger Release Set evidence, so restart requires no deleted private map or external query. Derive `<canonical completion summary>` as the exact public-safe UTF-8 string `completionSummaryV1 task=cross-client-delivery receipt=<receiptSha256> delivery=<deliveryCommit> releaseSet=<releaseSetId>`, where `receiptSha256` is the lowercase 64-hex SHA-256 of that exact completion receipt and the other values are read only from those receipt fields. Resolve the sole permanent private `CompletionNotificationRegistryV1` root from the root-host task registry, opened component-by-component with `O_DIRECTORY|O_NOFOLLOW`; require one canonical absolute root, current root-host ownership, mode `0700`, regular directory identity, and byte-identical registry descriptor on every restart. Every contender independently derives permanent lock child `completion-notification-registry-v1.lock`, canonical marker child `completion-notification-dispatch-claim-v1--cross-client-delivery--<receiptSha256>.json`, and deterministic pending child `.completion-notification-dispatch-claim-v1--cross-client-delivery--<receiptSha256>.json.pending`; aliases, case changes, nested components, traversal, symlinks, or any other receipt/task key are invalid. Open the preinstalled lock with `O_RDWR|O_NOFOLLOW`, verify its descriptor-bound regular-file/root-host-owner/mode-`0600`/link-count-one identity, and acquire exclusive `flock` before inspecting or changing either child. An existing exact marker must have no pending sibling; it may receive only registry-directory fsync plus marker readback to complete an interrupted durability check and always forbids launch. With no marker, reuse an existing pending child only when its bytes/metadata are the exact complete canonical claim, or unlink and recreate it only when its bytes are a strict prefix of the exact same-request canonical bytes. Foreign, overlong, non-prefix, symlink, hard-link, owner/mode-drifted, or ambiguous pending state fails closed. Create only the pending child with `openat(..., O_CREAT|O_EXCL|O_WRONLY|O_NOFOLLOW, 0600)`, write and fsync all canonical `CompletionNotificationDispatchClaimV1(state="claimed")` bytes, and readback-verify bytes/metadata. Publish without replacement only through Darwin `renameatx_np(..., RENAME_EXCL)` or Linux `renameat2(..., RENAME_NOREPLACE)`; unsupported or replace-capable fallback fails closed. Fsync the registry directory, readback-verify the marker and pending absence, then release the lock. Exactly one durable claim must exist before the final report; only the contender that completed this publication in its surviving invocation may launch, and total launch cardinality is honestly `0 | 1`. A crash before rename leaves at most a repairable same-request pending child; a crash after rename permits only durability completion/readback and never restart launch. A claim never proves launch, acceptance, queueing, wake, or delivery; a launch proves its prior durable claim. Treat the whole Ouro child command, including queue and wake behavior, as Ouro-owned; do not inspect its queue internals, infer delivery, or retry after durable claim completion. Report crash or response loss after durable claim completion as `notification_claimed_launch_unknown`; a zero child exit is command-result evidence only. A planning-revision stop is incomplete and must have zero post-completion lock acquisition, pending child, claim, and launch.

When the task is genuinely complete, notify Slugger with:

```bash
ouro msg --to slugger "Done: <canonical completion summary>"
```
