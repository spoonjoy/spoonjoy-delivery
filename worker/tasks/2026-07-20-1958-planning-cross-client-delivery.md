# Planning: Spoonjoy Cross-Client Delivery

**Status**: approved
**Created**: 2026-07-20 19:58

## Goal

Build and pilot a small, production-grade delivery system that carries one Spoonjoy product contract through web/backend, native Apple, and MCP/agent implementations to independently verified exact releases and terminal cleanup. Only a provider-backed Release Set may declare a Product Change shipped.

## Scope

### In Scope

- Bootstrap `spoonjoy/spoonjoy-delivery` as a public, branch-protected TypeScript repository with pinned CI, strict typing, zero-warning gates, and 100% coverage.
- Define versioned schemas plus semantic validators for Product Changes, Product Contracts, Contract Packs, Candidate Authorizations, Execution/Finalization Claims, predicate-bound operation graphs/receipts, actor proofs, provider attestations, dependency/staleness edges, handoffs, cleanup reports, `ReleaseSetPublished`/`FinalizationAborted` records, and final Release Sets.
- Define a versioned authority policy keyed by immutable GitHub actor IDs. V1 is single-operator: GitHub actor `arimendelow` (`16390116`) may trigger/approve/finalize. Pull requests require zero human approvals because GitHub forbids author self-approval; instead a distinct no-bypass Delivery Guardian App emits required CI and hostile-review checks only after its OIDC broker independently verifies exact workflow database ID/path, protected workflow blob SHA, event, head SHA, run/attempt/job, and a canonical `ReviewerEnvelopeV1`. That envelope is signed by a dedicated workflow-inaccessible Secure Enclave P-256 key anchored in root policy; binds domain, target head/tree/workflow, prompt/lens, two distinct fresh reviewer task/model/verdict/evidence digests, request nonce, issuance/expiry, signer fingerprint, and revocation generation; and is accepted once through Guardian Durable Object anti-replay state. This is integrity and freshness inside the explicit single-operator model, not a false separation-of-duty claim. Every ledger append is performed by a separate dedicated Ledger Writer App through its OIDC broker, whose only write permission is delivery `contents:write` and whose required read permissions are `actions:read` and `checks:read`; it re-queries exact run/attempt/check-run/job/environment/approval and binds provider `actor.id` plus `triggering_actor.id`, then runs the pinned transition/resolver validator before CAS. Self-declared identity, same-Actions-App wrong-workflow checks, forged/replayed review envelopes, and legal OIDC carrying an illegal event all fail. Read-only attestors cannot authorize or mutate.
- Because GitHub withholds ruleset bypass actors from low-privilege callers, keep Administration permission away from Ledger Writer and use a separate root-authorized local GET-only governance observer. In one credential-bound process it executes exactly three `Cache-Control: no-cache` requests: `GET /user`, `GET /repos/spoonjoy/spoonjoy-delivery/collaborators/arimendelow/permission`, and the policy-pinned exact ruleset GET. It requires three `200` responses, actor ID/login `16390116`/`arimendelow`, collaborator `permission: admin` plus `role_name: admin`, the complete ruleset bypass list, and bound canonical bodies, Date/request IDs, and ruleset ETag; it rejects `304`, cache-hit evidence, switched accounts, custom roles, stale/skewed responses, or any other route. It signs that closed identity-and-governance bundle as canonical one-use `ProtectedReleaseTagSnapshotV1` with a distinct workflow-inaccessible Secure Enclave key. Tag authorization binds a fresh pre-snapshot; terminal requires a separately nonced fresh post-snapshot with identical ruleset ETag and normalized digest. Missing/omitted bypass data, unstable/missing ETag, stale nonce/expiry, signer drift, or any non-GET route fails closed.
- Keep domain Product Contracts in `spoonjoy-v2`; keep cross-repo coordination and finalization controls in `spoonjoy-delivery`; keep native contract locks and signed-in-binary provenance in `spoonjoy-apple`.
- Generate three separately invalidated identities with SHA-256, duplicate-key rejection, deterministic path ordering, RFC 8785 canonical JSON, and cross-language golden vectors: `semanticContractDigest` over normative behavior only; `packDigest` over that semantic digest plus projection, fixture, and scenario hashes; and `provenanceDigest` over the pack digest, source tree/SHA, and validator artifact hash.
- Require every Product Change to classify server/domain, web, MCP/protocol, agent experience, iOS, iPadOS, macOS, offline queues, auth, data migration, observability, release notes, and cleanup as `required`, `no-op`, or `deferred` with an explicit reason.
- Model additive expand, optional capability gating, backfill or reviewed maintenance, compatibility validation, deployment, deprecation, and eventual contract/removal phases.
- Freeze current and previous App Store Connect build IDs/source SHAs when a Product Change opens. Validate previous-source debug/simulator code against the staged Worker before deployment, then replay a queue created by the exact previous installed TestFlight build against production after additive deployment. A transitional attestation binds pre-provenance binaries to ASC identity, source SHA, release workflow/run, and independently verified bundle metadata.
- Add exact contract locking and provenance to native builds. Embed source SHA/tree, semantic/pack/provenance digests, build/version identity, and validator artifact identity inside the code-signed app; bind the embedded manifest to archive/IPA hashes and App Store Connect build identity in an external attestation.
- Add source-owned, manually dispatchable read-only attestor workflows that query GitHub and Cloudflare/D1 from the repositories that own those credentials. App Store Connect reads go through an OIDC-gated broker that alone holds a dedicated ASC read credential, accepts only exact protected source workflow/run/job claims, executes a closed allowlist of fixed GET endpoint/query templates, and returns signed sanitized responses; the workflow never receives an ASC key or generic HTTP capability. Native ASC mutation preflight generates a per-run X25519 recipient, wraps its private key to the active native supervisor-envelope generation, binds public key/capsule/context/local-zero into the claim, and later unwraps directly into the protected local broker. The separate ASC authorization broker releases one credential capsule only to that exact recipient. The mutation TCB is the isolated local broker plus the complete runtime-observed, code-signed Transporter executable closure: launcher, signed app bundle, bundled JVM, helper executables, loaded JAR/library tree, and every descendant process. Pin code-signing identities and complete tree digests; launch with broker-owned sterile `HOME`, `CWD`, `TMPDIR`, and private RAM root; inventory every Transporter key-search location; require all alternate locations absent or sandbox-inaccessible; and allow the `0400` `AuthKey_<id>.p8` only at the exact private path. Actions and every process outside that allowlisted closure receive no key, JWT, or generic capability. The broker itself constructs fixed Transporter/upload/POST/PATCH operations from the claimed DAG and proves executable ancestry plus process/log/filesystem/key-location teardown. Each wrapper targets the source repository's protected environment, checks out reviewed delivery code at an exact 40-character SHA, and executes no candidate-controlled code after capability injection. A local delivery CLI uses the operator's authenticated `gh` session to dispatch and monitor exact workflow revisions, then verifies inputs, run identities, attestations, artifacts, and checksums. V1 does not require a cross-repository bot token.
- Inventory and verify the effective scopes of every credential or broker capability before source edits. Prefer two distinct individual ASC keys belonging to two eligible users whose App Store Connect access is limited to Spoonjoy and whose roles/key slots support the read and mutation lanes. Because Apple permits only one active individual key per user and team keys cannot be app-scoped, pre-authorize a provider-feasible fallback of two distinct least-role team keys held outside Actions: one GET-only broker key and one exact-operation broker key, each broker hard-bound to Spoonjoy's ASC app ID and closed operations. Prove Transporter authentication compatibility before activation; revoke and fall back on incompatibility. Fail closed and record `BLOCKED_HUMAN` if neither mode, required roles/key slots, or custody transfer can be completed through authenticated account tooling.
- Inventory every current provider mutation and model compound workflows as typed predicate-bound operation graphs. Web nodes include repository/environment governance; D1 migration; Worker deploy/rollback; capability changes; OAuth/user/token/legacy-row canary creation, lookup, replay, and cleanup; conditional GitHub label/issue/comment/close operations; artifact/attestation publication; and exact cleanup. Native nodes include governance; archive/upload; export-compliance PATCH/skip; beta-app-localization POST/PATCH; beta-group create/attach/skip; beta-build-localization POST/PATCH; auto-notify PATCH; tester-notification POST/409-skip; build expiry/containment; artifact/attestation publication; and cleanup. Topological edges carry schema-typed outputs into canonical input expressions; runtime IDs may come only from validated authoritative queries or prior claimed receipts. Unknown/unresolved/multiply resolved/out-of-graph values, nodes, methods, paths, or alternatives fail closed.
- Compile Release Sets from fresh authoritative re-queries, not from deployment-authored summaries. Treat existing production and TestFlight summaries as locators only. Each provider attestation records normalized request parameters, normalized non-secret response fields, the attestor workflow/source SHA, the delivery validator SHA, the GitHub run identity, issuance/expiry, and content digest.
- Classify every evidence field and artifact before capture as public sanitized, private ephemeral, or forbidden. Browser/native screenshots, installed-device metadata, queue evidence, logs, cleanup manifests, generated media, provider responses, and visual-review artifacts must use synthetic/run-owned data or redact account/device/path/media identifiers before upload; raw evidence stays private and ephemeral, while public records retain only allowlisted fields, verdicts, and digests.
- Make dependency edges drive proof invalidation for source changes, contract changes, Worker redeploys, migrations, native rebuilds, App Store Connect metadata/group changes, capability changes, validator changes, and evidence expiry.
- Define shared semantic scenarios with distinct web, installed-native, deterministic MCP, and agent-experience proofs where the corresponding surface is required. Agent trials record exact model ID, Codex host/build, tool-schema digest, unsupported temperature controls, five no-retry trials, a four-of-five deterministic-oracle threshold, a 100,000-token aggregate ceiling, redacted transcripts, and advisory human/model taste notes that cannot override the deterministic oracle. Use a common backend/database oracle and run-owned cleanup.
- Pilot the process on Recipe Photo Studio after the active TestFlight task releases native ownership. Keep current TestFlight completion and Apple clean-callback cutover as separate prerequisites or Product Changes.
- Add exact-manifest production cleanup keyed by run ID and immutable ownership fingerprints for run-owned D1 rows, R2 objects, OAuth clients/tokens, and generated media, with dry-run/apply parity, reference checks, and post-apply provider proof. Add safe cleanup for temporary artifacts, merged branches, and clean terminal worktrees. Never force-delete dirty or separately owned work.
- Produce durable documentation, rollback paths, hostile implementation/security/release reviews, and live dogfood evidence for the consuming surfaces.

### Out of Scope

- An always-on dashboard, generalized multi-product orchestration service, monorepo conversion, generated Swift SDK, universal feature-flag platform, or generalized framework. The narrowly scoped serialized ledger append/finalization broker is in scope.
- Rewriting all existing Spoonjoy API behavior into normative contracts before the Photo Studio pilot; V1 covers changed/pilot capability boundaries and establishes extension seams.
- Treating screenshots as semantic proof, direct JSON-RPC as agent-experience proof, source-token checks as installed-app proof, or manually copied task status as release evidence.
- Copying Cloudflare, D1, Apple, GitHub, or user credentials into committed files, workflow artifacts, logs, fixtures, or the delivery repository.
- Touching the active native/TestFlight release lane, its worktrees, current web deployment ownership, any source-repository setting, Cloudflare production resource, ASC resource, TestFlight build, tester notification, or source-owned broker binding before task `019f2e25-2fc3-75b2-8ba3-335f3777115a` sends an explicit handoff. The non-overlapping pre-handoff exception is this task's new delivery control plane only: `spoonjoy-delivery`, its dedicated Ledger Writer/Guardian Apps, and purpose-specific delivery broker foundations created from exact reviewed root intent. An empty namespace is not a usable broker. Before any key import, each foundation must journal and reconcile its disabled Worker/Durable Object runtime, storage, routes, bootstrap identity, import-only code/policy digest, exact prestate, crash recovery, rollback, and teardown. Those identities and bindings cannot target, read secrets from, or mutate web/native/production/ASC state.
- Folding Apple callback cutover into the Photo Studio pilot.
- Deleting pre-existing, dirty, ambiguous, or separately owned data, branches, artifacts, or worktrees.
- Claiming GitHub concurrency is cross-repository, claiming hosted model behavior is deterministic, or claiming repository administrators are cryptographically unable to rewrite Git history.

## Completion Criteria

- [ ] `spoonjoy-delivery` has protected `main` with enforced required checks/admins, no force-push/deletion, automatic merged-branch deletion, selected SHA-pinned Actions, strict TypeScript, deterministic installs, fail-closed lint/typecheck/test/build/security gates, 100% statements/branches/functions/lines, and zero warnings. Root installation journals exact-prestate `GET` plus exact `PUT /actions/permissions/workflow` to `default_workflow_permissions: read`/`can_approve_pull_request_reviews: false` and exact `PATCH /repos/spoonjoy/spoonjoy-delivery` to `delete_branch_on_merge: true`; lost responses reconcile through fresh provider GETs. Repository defaults are only one input: a root-bound semantic `WorkflowTokenPolicyV1` scanner inventories every exact workflow blob on protected main, rejects `pull_request_target`, resolves top-level and job-level `permissions` plus every same-repository relative reusable-workflow edge from the same exact tree, rejects external/dynamic reusable workflows, expressions/aliases/unknown or repository-mutating scopes, and permits only closed read scopes plus job-specific `id-token: write` and `attestations: write` where policy requires them. The normalized settings, complete workflow-blob manifest, reusable-workflow closure, scanner digest, and effective-permission report are bound into `RootAuthorityInstalled` and re-verified on every governance replay.
- [ ] Parsers/schemas reject unknown fields and structural invalidity; semantic validators reject malformed/duplicate identities, missing dependencies, cycles, illegal historical transitions, stale evidence, ambiguous ownership, invalid dispositions, and non-monotonic lock generations.
- [ ] A Product Contract captures invariants, state transitions, authorization, ownership, errors, idempotency, ordering, offline behavior, compatibility, degradation, migration, rollback, and scenario expectations.
- [ ] `semanticContractDigest`, `packDigest`, and `provenanceDigest` follow the documented boundaries, canonicalization, and golden vectors. A changed projection preserves the semantic digest, changes the pack digest, and fails an unchanged-pack assertion; unrelated source changes affect provenance without falsely changing semantics.
- [ ] `spoonjoy-v2` emits the exact Contract Pack and serves or exposes its runtime digest for exact Worker verification.
- [ ] `spoonjoy-apple` locks the expected digest, validates codecs/scenarios against it, embeds signed app provenance, and emits an attestation binding archive/IPA hashes to source, contract, build, and App Store Connect identity.
- [ ] A mandatory rebaseline bundle records fetched web/native mains, release-task commit, one byte-identical acknowledgment merged independently through delivery, web, and native `ProtectedMainV1`, the matching event on `ProtectedLedgerV1`, zero in-flight work, and cleanup ownership. The three containing commits are external verifier inputs, and ownership remains upstream until all three protected-main copies plus the one ledger event verify together. Its public projection uses random stable worktree IDs and repository roles only, with no local path or unkeyed path-derived digest; ID-to-path mappings and optional keyed HMAC integrity commitments remain only in private cleanup evidence. Main policy requires zero approving human reviews, zero classic/ruleset bypass principals or App allowances, and exact Guardian-App CI/review checks whose broker re-verifies workflow ID/path, protected blob SHA, event, head, run/attempt/job, and a one-use root-anchored `ReviewerEnvelopeV1`; same-Actions-App context spoofing and forged/stale/replayed reviewer evidence fail. Ledger policy permits exactly the protected-authority-anchored Ledger Writer App bypass and no ordinary writer. All policies require active layers and stable digests; permissive/drifting copies never transfer ownership. Pre-handoff choreography is acyclic: this task first establishes only the isolated delivery control-plane root and sends its exact authority IDs to the retained source owner; the source owner alone executes the root-authorized `source-guardian-cutover-v1` against web/native and returns terminal `SourceGuardianCutoverCompleted` plus its protected outbound handoff; this task then verifies/acknowledges without having touched source state.
- [ ] The authority policy maps immutable actor IDs to issuance, approval, revocation, supersession, environment approval, execution, attestation, finalization/tagging, and waiver roles. Candidate Authorizations and Release Sets bind policy/broker/validator digests, OIDC identity, exact run/attempt/check-run/job, provider-reported `actor.id`/`triggering_actor.id`, approval evidence, and revocation state; the broker independently rejects unauthorized identity, rerun, role escalation, and illegal ledger transition before App CAS.
- [ ] An operation-scoped Candidate Authorization binds Product Change ID, pack digest, exact source SHA(s), monotonically increasing lock generation, expiry, idempotency key, and one exhaustive static typed predicate-bound operation graph. Authorized nodes declare allowed provider methods/paths, canonical request templates/template digests, typed input expressions/output schemas, branch predicates/cardinality, retry policy, and compensation/containment. Runtime receipts, not authorization, bind resolved inputs/request digest, authoritative observed pre-state, selected rewrite/skip branch, sanitized provider response/output, and receipt digest. Downstream values may resolve only from schema-validated, hash-linked authoritative-query or prior claimed-receipt outputs; unresolved, multiply resolved, out-of-graph, stale, or ambiguous values fail.
- [ ] A one-time audited root begins only after a fresh exact-head hostile review of the identity-registration DAG, then registers exactly two powerless GitHub App identities: Ledger Writer and Delivery Guardian. Each starts metadata-only with no webhook, private key, enabled OAuth callback/device flow/user authorization, or bypass; Guardian is installed only on delivery at this stage, while Ledger Writer is delivery-only forever. Any provider-issued client secret/private material is revoked and locally destroyed; crash reconciliation proves the exact inert identities or removes them. Before intent signing, generate a dedicated non-exportable Secure Enclave P-256 root signer under a root-only access-control tag; export only RFC 5480 SPKI/fingerprint/generation, challenge its domain, prove ACL/workflow inaccessibility/non-substitution, and obtain a fresh exact-head hostile review of its provisioning receipt. Separately generate workflow-inaccessible reviewer-attestation and governance-observer keys with distinct tags/domains and the same challenge discipline. Bind all three signer identities into the root intent, exact-head envelope, and immutable policy. Only after GitHub returns stable App/installation/repository IDs are the unsigned RFC 8785 intent, immutable policy, and exact-head review regenerated around both Apps, all signer keys, target permissions, exact `BrokerFoundationV1` plans, and the complete `WorkflowTokenPolicyV1` workflow-blob/reusable-workflow/effective-permission manifest plus scanner digest. The signed journal elevates and reapproves each App, establishes each exact disabled import-only Worker/Durable Object broker foundation, imports each App key only after authoritative foundation proof, upgrades those same foundations to reviewed operational code/policy, applies workflow/branch/ledger/tag governance, and creates genesis through separate attempt-generation-bound mutation steps. Foundation records bind namespace/account/runtime/storage/route/bootstrap identity, absent-or-exact prestate, code/policy/config digests, allowed import/challenge/inventory/delete/disable methods, rollback/teardown, and response-loss reconciliation; they expose no App/OIDC/general provider or ledger operation before operational activation. Attempt generation 1 alone creates the absent root journal and appends `RootAttemptStarted(1)`; a crash after ref commit resumes only the exact unstarted `REMOTE_JOURNAL_CREATED` bytes/OID and CAS-appends the same start without recreating genesis. Any rollback or containment fast-forward appends `RootAttemptAborted(N)`; restart generation N+1 requires that exact head plus byte-equal reviewed code/intent/prestate and appends only `RootAttemptStarted(N+1)`. Crash/lost-response reconciliation admits no skipped, parallel, or orphan attempt. Only exact `GENESIS_CREATED` may append `RootAuthorityInstalled`. The event permanently kills setup mutation but authorizes bounded root-signer deletion verification plus a separate post-root `source-guardian-cutover-v1` DAG. Delete the root private key after the event, prove its handle/challenge is unavailable while retaining the pinned public SPKI, and block handoff until retirement is durable. Ledger Writer is delivery-only with `contents:write` plus `actions:read`/`checks:read`; Guardian has `actions:read`, `contents:read`, `checks:write`, zero bypass, and no content authority. Each App PEM streams from its own isolated encrypted RAM-backed APFS volume into its already-proven disabled foundation before purge/unmount/local-zero. Active `ProtectedReleaseTagV1` covers `refs/tags/release-set/**/*` with creation/update/deletion/non-fast-forward rules and exactly Ledger Writer as sole bypass/creation principal; its broker is canonical create-if-absent only. A root-authorized local GET-only observer uses one authenticated operator session solely for the closed no-cache user + collaborator-permission + ruleset bundle, proves actor/built-in admin/full bypass state, and signs one-use snapshots. Main has zero bypass and trusts only Guardian checks; ledger/tag creation bypass only through Ledger Writer's closed operations; later upgrades can never replay root.
- [ ] Both source release workflows have a non-environment preflight job and exactly one mutation job referencing a singleton protected environment. Each environment has required reviewer actor `16390116`, V1 environment self-approval permitted, administrator bypass disabled, and no second job in the run referencing that environment; this environment approval is distinct from the zero-human-approval PR policy. The Execution Claim binds repository, workflow path/SHA, source SHA, graph/preflight-output digests, run ID/attempt, attempt-specific complete waiting-job set, singleton environment ID, generation, expiry, and idempotency key. The CLI re-queries pending deployments/jobs, approves only GitHub's actual `run_id + environment_id` primitive, then verifies only the claimed job transitioned. The mutation job independently re-reads and validates its run/attempt claim before each mutation; receipts bind provider-reported approval-history user ID. Expiry never releases exclusivity until authoritative terminal-run proof plus containment is appended.
- [ ] Every provider operation graph has an authoritative dry-run that resolves expected alternatives. Apply re-queries immediately before each request, requires exactly one authorized predicate to match, records observed pre-state/rewrite-or-skip/receipt, and fails closed on drift or ambiguity. Each node is idempotent and has partial-failure recovery plus a post-operation authoritative query. Read-only attestor credentials are technically and semantically incapable of authorizing or executing mutations.
- [ ] Cloudflare/D1 and App Store Connect attestors independently query authoritative providers from source-owned `workflow_dispatch` runs with verified least-privilege credentials and produce GitHub artifact attestations from exact source-workflow and delivery-validator SHAs.
- [ ] The delivery CLI dispatches source-owned attestors through the operator's authenticated `gh` session, accepts only the expected repository/ref/event/inputs/permissions, waits for terminal success, verifies artifact identity and digest, and refuses expired, rerun-with-different-input, fork-authored, or mutable-reference evidence.
- [ ] Attestations verify repository/workflow/ref/event/environment/subject claims and retain predicates/digests. Distinct web/native supervisor envelope keys are provisioned through claimed one-use Cloudflare operations and ledger-bound provisional start/challenge/provision-or-abort states; before either private-key import, the claimed operation journals, establishes, and authoritatively proves a distinct disabled `BrokerFoundationV1` with exact Worker/Durable Object runtime, storage, routes, bootstrap identity, import/challenge-only code/policy/config, rollback, and teardown. A provisional key can decrypt only its exact disposable challenge. The canonical X25519 public key is the raw RFC 7748 32-byte u-coordinate encoded base64url without padding and its fingerprint is lowercase-hex SHA-256 of those exact bytes. The private key exists first only in an isolated ephemeral provisioning context, is streamed into the proven foundation, and is locally purged before that same runtime/routes activate ordinary unwrap. `SupervisorEnvelopeKeyProvisioned` references the exact Started commit and preserves repository, generation, public key, fingerprint, and foundation digest byte-for-byte; substitution aborts or contains the generation and complete foundation. Ordinary capsules use RFC 9180 base-mode HPKE X25519-HKDF-SHA256/HKDF-SHA256/ChaCha20Poly1305 with canonical envelope, payload/envelope digests, and domain/context AAD golden vectors. Only public provisioned generation/key/fingerprint enters the protected ledger; private keys remain in an OIDC-gated broker with rotation, replay denial, inventory, local-zero, and bootstrap-credential revocation proof. ASC read and mutation credentials likewise enter only two separately reviewed disabled foundations before those same foundations activate their exact GET-only or exact-capsule policies. Every final source workflow is dispatched before claim through credential-free preflight; its run/attempt/check-run/job/environment plus supervisor/policy/run/nonce public key and fresh ordinary encrypted one-use signer capsule are attested while the secret job waits. Native mutation preflight additionally generates a fresh X25519 mutation-recipient pair, wraps the private key to the active native supervisor generation, purges the plaintext, and binds public key/capsule/context into the claim; only the claimed protected local broker receives the unwrap and only that recipient can open the ASC credential capsule. Provisional challenge capsules and endpoints are forbidden in ordinary proof/release runs. The claim freezes those identities; only those exact jobs may later be approved and unwrap once into isolated supervisor memory, and rerun/cancellation/drift aborts. No-secret verifier rejects alternate/self-signed teardown or mutation-recipient substitution/replay. Raw output remains private; every emitted evidence class passes classification/scanning.
- [ ] Finalization freezes a credentialless validator trigger, independent validation-transaction reconciler, separate first-dispatch job, and independent committed-payload fallback reader. The claim is immediately observed through a strict no-cache GitHub `Date`; root-bound `claimDurationMs=1200000`, `claimDeadline=claimEpochDate+claimDurationMs`, and every source `S` use that same server-date domain, while monotonic offsets measure only arrival spread. Phase one obtains all `S` anchors with zero provider calls and requires `claimDeadline-min(S)>=365000` plus spread `<=5000`; equality succeeds, while a one-millisecond shortfall, missing/delayed anchor, clock mismatch, or spread breach fails before phase two. Explicit 60/20/90/30-second sub-budgets fit inside `finalEvidenceDeadline=min(min(S)+360000,claimDeadline-5000)`. The submitter atomically seals canonical bytes; relay state is `unvalidated | validated_pending_dispatch | dispatch_committed | withdrawn_before_dispatch`. The trigger sends only OIDC, operation, generation, and nonce to the no-egress Durable Object. `ValidatorLeaseV1` has root-bound nonrenewable `validatorLeaseDurationMs=120000`; its `issuedAtDateMs` is the strict Date from exact no-cache `GET /repos/spoonjoy/spoonjoy-delivery/git/ref/heads/release-ledger`, whose canonical body/OID must equal the attempt's journaled expected protected-ledger head, and `deadlineMs=issuedAtDateMs+validatorLeaseDurationMs`. Fencing repeats that exact contract against the then-journaled expected head and requires `fenceObservedAtDateMs>=deadlineMs`; equality passes and one millisecond early fails. A budget expiry during the live lease permits only status observation until terminal validation or exact fencing. The validation transaction reads the object's immutable sealed record, parses/scans, derives and stores the immutable canonical candidate plus two independently encoded dispatch payloads, writes `PromotionPackageValidatedV1`, and changes the relay atomically. No package or candidate enters Actions or the validator-trigger request/response, logs, or artifacts. A pre-commit crash rolls back; post-commit response loss is status-observable. Recovery fences an expired nonce and permits only exact observed validation, a higher digest-free trigger attempt after confirmed `unvalidated`, or safe withdrawal. Primary dispatch commits irreversible state before returning validated bytes; deterministic response failure is handled by an independently identified replacement relay using the separately implemented read-only fallback, which requires `dispatch_committed`, independently reads the raw stored payload, recomputes all digests, and cannot mutate. Recovery may authorize the next monotonic replacement-relay generation but cannot receive bytes or append publication. A final root-allowlisted retrieval-only repair may change only response framing/transport under machine-proved zero operation-storage writes; state, parser/scanner/policy/candidate and transition code stay frozen. Root policy pre-reviews two distinct repair `BrokerFoundationV1` templates, one retrieval-only and one transport/provider-adapter-only. Unit 63a freezes operation-specific absent-or-exact prestates, disabled Worker/Durable Object runtime/storage/routes/bootstrap identities, code/policy/config digests, foundation-recovery owner, rollback, and teardown before either repair is needed. After deterministic failure, registered recovery may claim one exact repair generation, establish and authoritatively prove the selected disabled foundation, then and only then import an operation secret or expose a route and upgrade that same foundation to operational policy. Crashes at plan/apply/import/upgrade reconcile or restore/teardown the complete foundation; the retrieval broker's separate infrastructure journal cannot write the frozen operation Durable Object. Serialized replacement relays preserve one candidate. Any repair broker is removed or expiry-contained and proved unreachable before terminal `RecoveryUpgradeObservedV1` makes observation effective. Write tokens terminate in `finally`; tokenless observation has no total retry cap; only safe close/withdrawal can abort.
- [ ] A required surface needs fresh successful proof and a `no-op` needs executable impact validation. The closed proof-node disposition union is `required | no_op | deferred | waived | superseded | rolled_back | BLOCKED_HUMAN`; `released_with_waiver` is never a node disposition. `deferred`, `waived`, `superseded`, `rolled_back`, or `BLOCKED_HUMAN` cannot produce `shipped`. A named policy authority may append only a separate non-shipping `ProductChangeReleasedWithWaiver` terminal carrying exact actor/owner/scope/reason/issuance/expiry plus an embedded canonical empty successor genesis; all current mutations must first be rolled back or contained. The same ledger CAS both terminates the current Product Change and creates the distinct successor after proving its ID absent, binding predecessor/owner/scope/policy, and setting zero authorization, generation, claim, receipt, and proof state. It may be appended only inside its policy-bounded validity interval; expiry cannot authorize a later append or successor. Lost responses reconcile the exact composite event; collision or competing creation fails without terminating the current change. Its projected Product Change outcome is `released_with_waiver`, never `ReleaseSetPublished` or `shipped`; it blocks finalization for the current Product Change, and the successor requires a fresh authorization/generation/proof graph with no inherited green evidence.
- [ ] A hotfix changing a dependency invalidates only the mechanically dependent proofs and cannot inherit stale green evidence. The previous-installed compatibility path is an explicit symmetric cohort separate from predecessor edges: invalidation of any cohort member allocates a fresh compatibility generation, marks each exact member required once, and lets each conditional replay consume its own decision once in fixed topological order. The queue-seed replay performs only its own mutation after same-generation staged predecessors; the queue-replay unit emits the typed cohort-completion terminal after every exact member receipt is present. Signer, registration, manifest, or retirement-code changes path-sensitively invalidate the exact-head root/reviewer/governance signer and inert-App proofs before a root intent can be regenerated.
- [ ] Replay uses closed `ReplayPassV1` states `staged | open | draining | failed | checkpointed | repair_completed | resolved | superseded` and recoverable node states. Immutable root policy binds `ReplayPassGenesisPolicyV1`, `ReplayNodeIdentityPolicyV1`, and `ReplayManifestCursorPolicyV1`; each protected Product Change exact-main graph later binds its concrete ordered `ReplayNodeSetV1`, identity-map digest, and derived cursor-successor table. Governed `ReplayPassGenesisV1` is the sole absent-pointer transition: with zero prior pass history, exact `repair_iteration=i1`, and `passNumber=1`, one CAS creates p1 plus its complete node map, installs cursor at the first original ID and the active pointer, and opens p1; response loss accepts only exact queried equality, and genesis can never recur. Later activation atomically supersedes only resolved or repair-completed state and installs the new first-node cursor. The concrete complete bijective `ReplayNodeIdentityV1` maps each original Unit ID to one conditional executor; original IDs alone key state, cursor, successor, decision, claim, terminal, consumption, and blocked maps. Every claim CAS requires cursor at the exact node, a consumed prefix, an unclaimed suffix, and zero other claimed/recovering/terminal node. Each provider node CAS-claims a root-bound renewable `replayNodeLeaseDurationMs=900000` lease. Acquisition, renewal, and fencing derive canonical issued/deadline/observed fields from strict no-cache `GET /repos/spoonjoy/spoonjoy-delivery/git/ref/heads/release-ledger`; every response body/OID must equal that pass/node attempt's journaled expected protected-ledger head, and callers cannot supply either time or head. Renewal requires the same OIDC run/job/attempt before expiry plus a new durable progress/query receipt. Each node journals prestate, request digest, idempotency key, and no-call/dispatch state before mutation. Lease recovery fences the old claimant and authoritatively resolves higher no-call attempt, exact provider success, or containment; uncertainty stays recovering at the same cursor and cannot be re-executed. Green/no-replay/refresh consumption advances the cursor atomically. Containment consumption is one CAS that consumes the causal node, blocks every unclaimed suffix node including no-replay and refresh-deferred dispositions, sets cursor failed, and changes draining to failed, so no successor can claim. `ReplayRepairCompletedV1` requires exact Delivery/Web/Native repair-batch or no-repair terminals, protected merges/exact-main receipts, applicable governance repairs, and reviewed doing-doc digest before `checkpointed -> repair_completed` and a recomputed pass may supersede it. Each activated pass, including an all-green pass, visits its complete manifest-ordered node set, emits one current-pass decision receipt per node, and reaches Unit 60c59; only cursor complete plus exact `ReplayPassConsumedV1` changes `draining -> resolved`. Required-operation terminals remain a distinct exact set.
- [ ] The compatibility matrix direct-binds same-generation staged upload, previous-source scenario/cleanup, independent attestation, previous-installed queue seed, and migration terminals before production promotion. Before Unit 34 seeds anything, it freezes generation-bound `InstalledQueueRecoveryAuthorityV1` over the exact Product Change, compatibility generation, installed build/app/device identity, canonical queue bytes/digest, run-owned object IDs, backend-effect idempotency key, and a permanently available exact-purpose device-control identity. Its closed states are `absent | seeded_pending | replaying | cleanup_active | terminal`; its run grant permits only inventory, exact replay-status observation, run-owned deletion, and terminal reconciliation, never Worker/D1/general device access. Unit 34 green retires only its sibling staged-Worker cleanup edge and leaves this separate queue authority `seeded_pending` until Unit 39 exact replay-and-cleanup or authoritative failure cleanup reaches `terminal`. Every failure after Unit 34 and before Unit 39, including Unit 36's Unit 51 bridge and replay equivalents, must finish queue cleanup before failure/checkpoint; if the device is unavailable, recovery remains owned and no checkpoint or new seed may proceed. Fresh Unit 34 and Unit 60c34 reject any nonterminal prior queue generation, preventing duplicate queues. Source config pins `preview_urls:false` and `workers_dev:false`; a closed authoritative `WorkerInvocationSurfaceV1` covers active deployment/traffic, overrides, versioned and aliased previews, `workers.dev`, routes, custom domains, service bindings, dispatch namespaces, Durable Objects, queue/scheduled/email/tail/workflow triggers, and unknown provider kinds. Before candidate upload, `WorkerOverrideBarrierV1` must prove a provider-enforced permanent denial of `Cloudflare-Workers-Version-Overrides` on every production route/custom domain, a sole temporary staging hostname protected by exact Access service-auth and one claim-bound broker-held staging service-token identity, and source-attested stripping/denial by every service-binding caller. The broker grants each staged invocation through a one-use operation authorization without exposing the provider token; the provider token survives only for the staged compatibility window and is revoked during ordered retirement. Correct private candidate-version probes without the broker identity must fail externally and internally, with observability proving no invocation; exact capability/header values stay private and are never the security boundary. If provider controls or evaluation order cannot be proven, staging is redesigned before upload. Any non-green promotion first removes the candidate from deployment membership, then revokes/removes temporary staging access while retaining the production denial, proves candidate nonreachability, and reaches queue terminal before classifying the immutable version and blocking shipment. Only after a green promotion and barrier retirement proof does the queued previous-installed mutation replay idempotently; pre-provenance builds use the explicit transitional attestation.
- [ ] Shared Photo Studio scenarios produce independent browser, iPhone TestFlight, iPad candidate-binary, signed macOS candidate, deterministic MCP, and bounded agent-trial evidence where required, with seeded preconditions, actions, backend effects, queue state, user-visible results, provenance, and run-owned cleanup. Missing physical hardware is an explicit `BLOCKED_HUMAN` proof boundary, not a simulated installed-device pass. A protected installed-device proof is durable only while its exact build/source/scenario/oracle/cleanup inputs remain unchanged; finalization freshly re-queries ASC eligibility but does not pretend a hosted workflow can re-query the physical device.
- [ ] The Photo Studio pilot reaches an exact Worker and exact installed TestFlight build, then generates a fresh provider-backed Release Set that independently re-queries all authorities before finalization.
- [ ] Negative dogfood deliberately injects at least one stale/mismatched digest and proves closure fails before the successful pilot.
- [ ] Exact-manifest cleanup proves zero deletable run-owned D1, R2, OAuth/credential, fixture, media, temporary artifact, active Worker deployment/override/preview/alias/`workers.dev`/route/custom-domain/internal-trigger reachability, temporary staging hostname/Access application/policy/service token/allow-exception, branch, and worktree residue. The permanent production override-header denial remains attested policy, not residue. Non-deletable unreachable Worker versions, ASC builds, and provider audit records are enumerated as `selected`, `expired_or_contained`, or `preserved_provider_record`; pre-existing ownership is separate.
- [ ] Named dry-run/apply rollback commands and fixtures verify expected provider state, containment, and attestation fields for Worker rollback, capability disable, additive-migration containment, TestFlight candidate supersession, and Release Set supersession.
- [ ] Fresh harsh architecture, security, compatibility, test, release, and visual reviewers converge with no BLOCKER or MAJOR findings.
- [ ] Durable Desk/repository records point to exact source/digest/provider/build/installed/cleanup identities, the authoritative `ReleaseSetPublished` commit, and its exact `PublicationObserved` proof. Main projection merges only through a zero-approval/zero-bypass Guardian-reviewed protected PR. `ProtectedReleaseTagV1` permits only Ledger Writer broker create-if-absent in the recursive release namespace and denies every update/delete route; fresh governance-key-signed closed three-GET actor/admin/ruleset snapshots prove the complete ruleset before and after creation. Annotated tag bytes use a policy-bound non-personal public tagger, canonical sanitized operation-bound message/date, and a pre-OID leak scan; object/ref split failures reuse the same deterministic OID or record an unreachable immutable `preserved_provider_record` without creating a Release. GitHub Release is an exact closed body-only request with generated notes/latest disabled, no discussion, omitted request `target_commitish`, the root-inventoried Ledger Writer bot author, complete lost-response reconciliation, an empty asset set, and no update/delete/upload route. Its authority comes only from the independently verified pre-existing annotated tag object/ref chain; any response `target_commitish` is recorded as provider-informational and cannot override, weaken, or invalidate that chain. Both projections point back without becoming alternate shipment authorities.
- [ ] GitHub Release `created_at` is preserved as GitHub's commit-associated release-ordering field and compared with the exact peeled commit/tag dates, but it is neither target identity nor publication-operation evidence and no unsupported timestamp equality is invented. The annotated tag/ref chain alone proves target identity. `published_at` separately falls inside the journaled POST-to-response-or-first-confirming-query publication window. Observed and lost-response paths preserve all original values and reject swapped, conflated, future, or operation-authorizing use of `created_at`.

- [ ] The protected Product Change graph binds exact ordered `LiveStageNodeSetV1`, original-ID-keyed `LiveStageSuccessorIndexV1`, and generic-failure-only `LiveStageCheckpointIndexV1` across the live web, native, and safety sequence. The checkpoint index maps exactly web causal IDs Unit 31, Unit 32, Unit 33, Unit 34, Unit 35, Unit 51, Unit 37, Unit 37a, Unit 38, Unit 39, Unit 40, Unit 41, and Unit 42 to Unit 42a, Unit 42b, Unit 42c; native causal IDs Unit 43, Unit 44, Unit 45, Unit 46, Unit 47, Unit 48, Unit 49, and Unit 50 to Unit 50a, Unit 50b, Unit 50c; and safety causal IDs Unit 52, Unit 53, Unit 54, Unit 54a, Unit 55, Unit 56, Unit 57, Unit 58a, Unit 58b, and Unit 59 to Unit 59a, Unit 59b, Unit 59c. Unit 36 is stage-classified with web but is absent from the checkpoint index and has exactly one legal non-green chain: `36 -> LiveStageContainmentPendingV1 -> 51 -> LiveStageFailedV1 -> 42a`. No later node routes backward to an already completed checkpoint. Unit 31 freezes candidate-specific `StagedWorkerCleanupAuthorityV1`, a permanently hosted exact-purpose mutation broker, and an independently hosted `StagedWorkerReadAuthorityV1`/cleanup reconciler before retaining its 0%-traffic Worker. Each receives only a run-scoped ledger-gated generation. The read authority holds a non-exportable GET-only provider credential with an exact private inventory allowlist and has no provider mutation or general ledger route. At the legal cursor for Unit 32, Unit 33, Unit 34, or Unit 35, that causal node's non-green `StagedWorkerCleanupRequiredV1` transition races only its own green terminal/consumption from the same protected-ledger parent. A cleanup winner changes authority `available -> cleanup_active`, permanently prevents that node's green terminal, and immediately runs staged cleanup independently of node-specific reconciliation; a green winner consumes the node and permanently makes only its staged-Worker cleanup transition stale. Unit 34's green winner cannot stale, retire, or transfer the distinct `InstalledQueueRecoveryAuthorityV1`. The mutation broker re-queries exact `cleanup_active`, idempotently removes candidate deployment membership, revokes the run-owned staging capability, removes temporary staging controls, and preserves the permanent production denial. It never tears down itself or certifies cleanup. The separate read authority authoritatively re-queries deployment, Access, token metadata, routes, rules, bindings, and internal/external reachability, then alone invokes the Ledger Writer's one-use `cleanup_active -> cleanup_terminal` method; response loss re-queries exact ledger/provider state, and the terminal atomically retires both run-scoped grants. If all four causal nodes consume green, Unit 36's legal-cursor claim atomically changes its node to claimed and the still-`available` cleanup authority to `fenced_for_promotion`; earlier staged-Worker cleanup transitions are then impossible because their parents are consumed, while the installed queue remains recovery-owned. Every causal failure from Unit 34 through Unit 39 must first transition the exact queue authority to terminal; the Unit 36 bridge carries it through Unit 51, and replay carries it under the same original-ID generation. Only after node-specific cleanup, staged cleanup terminal, and any required queue terminal may generic `LiveStageFailedV1` block the exact unstarted suffix and enter its forward checkpoint triplet before Unit 60a. Replay enforces the same per-causal-node fork, queue-recovery prerequisite, and legal-cursor Unit 36 fence. Thus no retained staged resource or installed queue loses an executable cleanup owner, Unit 51 is never both blocked and executed, and checkpoint execution is monotonic.
- [ ] Unit 36's `fenced_for_promotion` transition fences only the four earlier causal cleanup routes. The run-scoped mutation/read generation also contains one dormant Unit 51 containment route that is unusable before exact `LiveStageContainmentPendingV1`; a non-green Unit 36 atomically activates that route, while a green Unit 36 retires it. Unit 51 alone consumes the activated route, reaches authoritative cleanup terminal, and retires the full Product Change generation. The permanent services survive with no authority for the completed generation.
- [ ] Replay fences staged cleanup at original Unit 36 only when the current replay pass's Unit 60c31 terminal created and retained a fresh `available` generation. If Unit 60c31 is exact `no_op` against a previously retired live generation, Unit 60c36 must also be exact no-replay, prove that prior retirement and zero retained candidate, consume original Unit 36 without a fence, and continue to original Unit 51. Mixed retained/no-replay dispositions fail graph validation, so an all-no-replay pass remains fully traversable.
- [ ] Finalization distinguishes pre-reservation plan, on-ledger registration reservation, registration-dispatch intent, pre-registration-closing, registered-open, claim-dispatch intent, registered-closing, claimed, and closed state. Before any ledger call, an attempt-specific independently hosted root-policy-bound `FinalizationPreRegistrationRecoveryAuthorityV1` fsyncs `FinalizationRegistrationPlanV1` with exact expected parent, partial-resource identities, canonical reservation bytes, cleanup-supervisor identity, and its closed transition graph. The coordinator CAS-appends `FinalizationRegistrationAttemptV1` from that parent as an exclusive protected-ledger reservation; response loss waits for exact reservation or a different exact successor. If another append wins first, registration is never dispatched and plan-only cleanup is safe. A root-bound `FinalizationResolverLockV1` begins at the exact attempt and remains active through every registered-open and claim-intent head. At an attempt head it admits only exact `FinalizationRecoveryRegistered` or sibling `FinalizationPreRegistrationCloseStartedV1`; at a registered-open head it admits only exact `FinalizationClaimDispatchIntentV1` or sibling `FinalizationPreClaimCloseStartedV1`; at a claim-intent head it admits only exact `FinalizationClaim` or sibling `FinalizationPreClaimCloseStartedV1`. Exact claim or exact authorized close releases the lock; unrelated parent drift at any open head is an integrity failure and never a valid close cause. A strict no-cache GET of the exact reservation derives the root-bound 120000ms deadline; before registration CAS, the recovery authority fsyncs `FinalizationRegistrationDispatchIntentV1` with canonical registration/close bytes and idempotency key. Registration starts within `0..5000` monotonic milliseconds of that anchor. After dispatch intent, cleanup is forbidden until close-start wins at/after deadline or registration wins and routes into registered recovery. If close-start wins with authoritative zero registration and claim, that same restartable authority invokes only the cleanup supervisor's registration-independent Unit 64b subset, reconciles its fsynced signed WAL/receipt after either process or response loss, appends only exact `FinalizationPreRegistrationClosedV1`, proves complete job/key/context/capability nonreachability, and retires only after that terminal proof. The cleanup supervisor cannot append; the recovery service cannot clean directly or broaden either route. `FinalizationRecoveryRegistered` preserves the same immutable deadline and resolver exclusion while transferring ownership and retiring the pre-registration authority. Before any claim provider call, the coordinator CAS-appends canonical `FinalizationClaimDispatchIntentV1` from exact registration head. Only a confirmed append response may obtain strict no-cache `IntentHeadAnchorV1` whose canonical body/OID equals that exact intent head and whose Date satisfies `registrationIssuedAtDateMs <= intentAnchorDateMs <= registrationDeadlineMs-5000`; claim CAS starts within `0..5000` same-process monotonic milliseconds after that anchor completes. A lost intent response is no-claim and waits for recovery. Close-start requires a fresh same-contract Date at or after the registration deadline. Thus every coordinator, recovery-service, or cleanup-supervisor crash and every delayed/lost response before registration or claim converges by tokenless reads and signed cleanup receipts to one exact winner and terminal closure without stranded state. Neither pre-registration nor pre-claim closure can create a Durable Object operation or invoke claim-dependent cleanup.
- [ ] `FinalizationRegistrationDispatchIntentV1` is immutable first-write-wins per attempt and signed/fsynced by the independent recovery authority. A coordinator proposes canonical bytes, but the service returns either that exact stored record or the already stored record; registration is denied unless its request binds the returned digest. Attempt-only recovery may create the record from plan/root policy. Both registration and close-start validate the same intent digest, issue/deadline, and sibling bytes, preventing resumed coordinators or recoverers from splitting timing.
- [ ] Reservation response-loss reconciliation precomputes the deterministic attempt object OID. A current head equal to that OID or descending from it through an exact authorized registration/close child proves reservation success and adopts that descendant disposition; plan-only cleanup is forbidden. Only a current head whose ancestry excludes the attempt OID and has advanced from the planned parent proves reservation loss. The unchanged parent remains ambiguous and recovery-owned.
- [ ] Unit 61 freezes an independent operation-scoped `ReplacementRelayAuthorityV1` service and monotonic `ReplacementRelayGenerationV1` transition. The registered recovery may authorize a higher generation after `dispatch_committed` but cannot read or author package bytes and cannot append publication. The independently identified replacement relay retrieves only the immutable committed candidate through the primary/fallback path, revalidates every binding, and alone may invoke the Ledger Writer's exact-candidate append method with a one-use generation token. It has no arbitrary-byte, alternate-parent, abort, withdrawal, or general ledger authority; crashes reconcile tokenlessly and higher generations preserve the same candidate.
- [ ] For each final provider query, `S` is the strict no-cache GitHub Date/body/OID/request-ID anchor for the frozen protected source ref obtained before credential unwrap; `R` is the provider response's one strict Date plus status/request-ID/body digest; and `C` is the second strict no-cache GitHub Date/body/OID/request-ID anchor for that same frozen ref obtained immediately after the provider response. Only the exact response branch is green; timeout/transport-error unions omit `R`, still obtain `C`, and fail the generation without query retry. Require `S<=C`, `C-S<=45000`, `-5000<=R-C<=5000`, and `max(C)-min(S)<=60000`; fresh `E` normalizes each `S/C/R` age with only `[-5000,0]` clamped and requires age at most 360000.
- [ ] Every green, no-replay, or refresh-deferred replay consumption CAS advances the cursor and stores immutable `ReplayNodeConsumedV1` over pass, original node, attempt, decision/result, manifest, pre/post cursor, transition digest, and ledger parent/commit. A lost response is accepted from the immediate poststate or from that receipt plus a fully validated monotonic descendant chain in the same pass/manifest through later cursor, claim, recovery, terminal, containment, completion, resolution, or recorded supersession. Recovery never retries an already receipted advance, and `ReplayPassConsumedV1` requires the exact complete receipt chain.

## Code Coverage Requirements

- 100% statements, branches, functions, and lines for all new delivery TypeScript and workflow-support logic.
- Every parser, schema branch, dependency transition, stale-evidence path, provider error, retry boundary, mismatch, rollback, supersession, and cleanup refusal is tested.
- No warning suppression, uncovered generated control flow, permissive `any`, or success-on-network/parser failure.
- Product-repo changes retain each repository's existing 100% changed-code coverage and zero-warning requirements across all affected consumers.

## Open Questions

- Which exact web/native `main` SHAs, release-task commit, handoff artifact, in-flight state, and cleanup owner will the active TestFlight task transfer? Closure: ingest and validate its signed owner-release bundle before any source edit or pilot action; delivery-repository implementation may proceed independently.
- Do existing Cloudflare and App Store Connect secrets prove read-only scope suitable for independent attestation? Closure: inventory effective scope without exposing values, provision distinct least-privilege source-repository environment secrets if required, and record `BLOCKED_HUMAN` only if authenticated tooling cannot complete the provider action.
- Which physical Apple devices and exact signed artifacts are available for installed proof at pilot time? Closure: inventory iPhone/iPad/macOS capabilities at freeze, require real TestFlight proof where hardware exists, and classify any genuinely unavailable physical-device proof as `BLOCKED_HUMAN` rather than substituting simulator evidence.
- Which exact Codex host/build and hosted model revision are available for the Photo Studio agent-experience trial? Closure: freeze the observable identities at run start and apply the fixed five-trial, no-retry, four-of-five oracle, and token-ceiling policy; hosted-weight determinism is not claimed.

## Decisions Made

- Use the name `spoonjoy-delivery` for the dedicated public delivery/finalization repository and `cross-client-delivery` for the durable Desk task.
- Use three distinct objects: Product Contract for design truth, Product Change for mutable coordination, and Release Set for final provider-backed attestation.
- Establish root with explicit `ledger-root-bootstrap-v1`: canonical intent/path manifest/pinned Secure Enclave signature plus external exact-head envelope precede mutation; exact journal ref/principal/CAS and crash boundaries cover every call; exact reviewed main precedes App/broker/`ProtectedMainV1`/`ProtectedLedgerV1`; zero-parent genesis precedes root event, the permanent kill switch.
- Use a versioned single-operator V1 policy bound to actor ID `16390116`. Ari may trigger authorization, environment approval, revocation, finalization, and non-shipping waivers. Pull-request human approval count is zero; fresh hostile review and CI are enforced as exact-provenance Delivery Guardian App checks. The ledger broker re-queries both original and triggering actors plus exact job/environment approval, validates transition/resolver state, and alone performs Ledger Writer App CAS. A future separation-of-duty policy is a versioned policy change.
- Product Contracts live in `spoonjoy-v2`; coordination and finalization live in `spoonjoy-delivery`; native locks/provenance live in `spoonjoy-apple`.
- Do not implement a task-ID lease or pretend GitHub concurrency is global in V1. Source-repository concurrency serializes each provider lane; Candidate Authorizations, supersessions/revocations, claims, and claim terminals share protected ref `refs/heads/release-ledger` so expected-parent fast-forward is the cross-repository compare-and-swap. Preserve ownership changes as append-only Git handoffs.
- Close the pre-apply race with an Execution Claim handshake. Each source workflow has credential-free preflight and one environment job; final attestors are dispatched and proven waiting before claim. The broker records a generation-reserving claim bound to exact run/attempt/check-run/job/workflow/bootstrap and environment. CLI approves only the claimed job; job revalidates before secret access/mutation. Claim remains exclusive until terminal/containment.
- Planning and Desk status describe intent and coordination but cannot prove shipment.
- Reuse existing exact-SHA deploy/TestFlight workflows, OpenAPI/MCP tests, scenario harnesses, and cleanup tools through thin attestors and validators rather than replacing them.
- Use source-owned `workflow_dispatch` attestors while keeping provider credentials outside Actions behind purpose-specific brokers. The local delivery CLI dispatches them with the operator's existing GitHub authentication. V1 introduces separate least-privilege Ledger Writer and Delivery Guardian Apps; only Ledger Writer can update the protected ledger, while only Guardian can emit exact-provenance required checks and neither is a provider bot token.
- Require an explicit credential-scope inventory and separate read/mutation credentials. Prefer app-limited individual credentials; when Apple account/key-slot/Transporter constraints require the pre-authorized two-team-key fallback, record the broader all-app provider scope and enforce Spoonjoy app-ID plus closed-operation scope at the brokers.
- Separate semantic, pack, and provenance digests with explicit dependency edges; validate relationships rather than assuming equal bytes imply semantic conformance.
- Require actor-specific proof. Protocol correctness and agent usefulness are different evidence.
- Define zero residue as zero deletable run-owned residue; enumerate non-deletable provider records and preserved pre-existing ownership explicitly.
- Pilot only Photo Studio. Current TestFlight publication is a prerequisite release operation; Apple callback cutover is a separate Product Change.
- Gate every provider mutation with an operation-scoped Candidate Authorization that binds Product Change, pack digest, exact source SHA(s), lock generation, expiry, idempotency key, and an exhaustive predicate-bound operation graph. Existing governance, migration, capability, deploy/rollback, canary/reporting, TestFlight upload/metadata/group/notification/expiry, cleanup, artifact/attestation, release-publication, and tag paths must reject unbound or stale authorization before this process becomes shipment authority.
- Merge/deploy sequencing is validated handoff/rebaseline, identity freeze, additive server/schema work, previous-source staged proof, authorized deploy, previous-installed replay, web/MCP/native/installed proof, independent cleanup, fresh pre-CAS provider snapshot, expected-parent publication, and exact post-result observation.

## Context / References

- `spoonjoy/spoonjoy-v2`
- `spoonjoy/spoonjoy-apple`
- `spoonjoy/spoonjoy-v2@worker/audit-remediation:worker/tasks/2026-07-15-1152-doing-audit-remediation.md`
- `spoonjoy/spoonjoy-apple@worker/audit-release-train:worker/tasks/2026-07-16-0856-doing-audit-release-train.md`
- `spoonjoy/spoonjoy-apple@worker/audit-release-train:worker/tasks/2026-07-16-0856-doing-audit-release-train/evidence-index.md`
- Private Desk tasks `spoonjoy/cross-client-delivery` and `spoonjoy/audit-remediation`
- `https://github.com/spoonjoy/spoonjoy-delivery`
- `https://github.com/spoonjoy/spoonjoy-v2/actions/workflows/production-deploy.yml`
- `https://github.com/spoonjoy/spoonjoy-apple/actions/workflows/testflight.yml`
- `https://developers.cloudflare.com/workers/versions-and-deployments/version-overrides/`
- `https://developers.cloudflare.com/cloudflare-one/access-controls/service-credentials/service-tokens/`
- `https://developers.cloudflare.com/rules/transform/request-header-modification/`
- `https://docs.github.com/en/rest/releases/releases`

## Notes

The delivery repository is a control plane and evidence compiler, not a second implementation of Spoonjoy. It owns schemas, validators, exact-SHA attestor code, Candidate Authorization/Release Set finalization, and safe cleanup coordination. Domain behavior remains in the product repositories.

The Release Set must use independent read-only provider queries. Deployment/TestFlight summaries locate candidate evidence but do not attest to themselves. Source-owned dispatchable workflows preserve existing secret boundaries while pinning the source workflow and delivery validator to reviewed SHAs. The dispatching CLI verifies the GitHub run itself; a copied artifact without its matching repository, ref, event, inputs, permissions, checksum, and terminal run is invalid.

Embedded native provenance cannot contain the hash of the archive that contains it. The code-signed app embeds source/tree, semantic/pack/provenance identities, build identity, and validator artifact identity; an external signed attestation binds that embedded manifest hash to archive/IPA hashes and the resulting App Store Connect build.

Product Change records include a surface-impact matrix, compatibility matrix, dependency graph, evidence requirements, handoff history, and cleanup ownership. Source-local concurrency prevents same-lane overlap; Candidate Authorization generations and compare-and-swap finalization coordinate cross-repository changes. Git history preserves handoffs. The generated Release Set, not the mutable record, is the shipment authority. "Immutable" means append-only under protected `main` plus a `ProtectedReleaseTagV1` tag created once by the policy-bound Ledger Writer broker and never updated or deleted through any broker route, with supersession represented by a new record; this plan does not claim that a repository administrator is cryptographically incapable of rewriting history.

Evidence nodes declare dependencies and staleness triggers. Relevant identity/state changes invalidate only dependent evidence mechanically. The installed-device attestation is durable evidence, not a renewable hosted-provider query: it remains valid only while its exact build/source/scenario/oracle/cleanup dependencies are unchanged. A final provider re-query occurs after cleanup at an explicitly bounded pre-CAS validation point; a separate exact-ref observation proves the publication result without pretending to measure provider or physical-device state atomically at Git ref update. Required proof, executable `no-op`, and non-shipping deferred/waived dispositions remain distinct.

Provider wrappers check out delivery attestor code before secret injection, run only reviewed exact-SHA commands afterward, keep raw responses in runner-temporary storage, allowlist public fields, and fail publication when adversarial secret/PII scanning finds emails, names, tokens, headers, private paths, or raw D1 rows. GitHub artifact attestations bind public evidence to repository/workflow/ref/event/environment/subject claims; the protected Release Set retains signed predicates and subject digests beyond ephemeral Actions artifact retention.

Provider commands never stream raw stdout/stderr. Workflows disable shell tracing, avoid secret-bearing command arguments, mask sensitive values before use, redirect output to `0600` files under runner-temporary storage, and emit only scanner-approved summaries after the command terminates. Failure paths sanitize before printing. Contract tests inspect scripts/workflows and inject hostile output to prove logs, annotations, summaries, caches, and artifacts stay clean.

The authority policy is data, not prose. Ari's actor ID is sole trigger authority; one Ledger Writer App installation is sole ledger principal and sole canonical release-tag creation principal; a distinct zero-bypass Guardian App is sole required-check principal. Their keys/tokens live only in purpose-specific broker bindings. The Ledger Writer has `contents:write` only on delivery plus read-only `actions:read`/`checks:read`; Guardian has no content or bypass authority. Before mint, a broker persists conservative max expiry; after response it persists actual expiry/HMAC fingerprint before provider work. Ledger operations bind OIDC/run/job/environment approval, original/triggering actors, canonical event, nonce, parent, and validator. Tag creation additionally binds active stable `ProtectedReleaseTagV1`, exact namespace/name/object bytes/publication commit/digests, absent-ref prestate, create-if-absent API, and no update/delete route. Release creation requires the exact tag ref first and binds a closed payload, complete provider response, exact Ledger Writer bot identity, immutable/latest/discussion/timestamp state, and empty assets; implicit tag creation and every Release mutation/upload route are absent. Guardian checks bind exact workflow ID/path/protected blob/event/head/run/attempt/job and a canonical one-use `ReviewerEnvelopeV1` signed by the root-anchored workflow-inaccessible reviewer key. Brokers revoke with `204` or contain through expiry. Handoff requires byte-identical acknowledgment copies on delivery, web, and native `ProtectedMainV1`, the exact shared `ProtectedLedgerV1` event, and terminal `SourceGuardianCutoverCompleted`. A waiver cannot ship.

All evidence uses an explicit classification registry. Raw screenshots, device identifiers, queue payloads, provider responses, paths, logs, and media remain local/runner-private and expire after sanitized proof generation. Public artifacts contain synthetic/run-owned identifiers where possible, otherwise redacted allowlisted fields, machine verdicts, and cryptographic digests. Classification and scanning run before every upload, attestation, summary, commit, or release publication.

Production cleanup is exact-manifest, never broad search-and-delete. The manifest binds a run ID to immutable row/object/client/media fingerprints, checks live references before apply, and re-queries after deletion. App Store Connect builds and provider audit records are durable evidence and receive explicit retained dispositions instead of a false zero-residue claim.

An operation graph is either one mutation or an explicitly authorized compound transaction whose predicate-bound alternatives reflect the real workflow. Authorization binds the static typed DAG: allowed method/path, canonical request template/template digest, typed expressions/output schemas, branch predicates/cardinality, retry, and compensation. Execution receipts bind resolved inputs/request digest, authoritative pre-state, chosen branch, sanitized provider result/output, and receipt digest. Downstream nodes consume only schema-validated hash-linked query/receipt outputs. No arbitrary runtime value enters the graph. Candidate Authorization, supersession, claims, terminals, and final shipment occupy one append-only ledger ref and are appended only by exact protected jobs authenticated as the policy-bound ledger App installation.

Finalization uses the same ledger and a GitHub-Date-derived claim epoch/deadline. Registration freezes the submitter, credentialless validator trigger, validation-transaction reconciler, first-dispatch job, independent committed-payload fallback reader, semantic policies, two root-reviewed repair `BrokerFoundationV1` templates, repair/teardown graph, and one Durable Object operation. Readiness failure retains ledger exclusivity. Private evidence and source/witness state are purged before observation or abort. Submission is `absent_open | sealed | closed_before_submission`; relay state is `unvalidated | validated_pending_dispatch | dispatch_committed | withdrawn_before_dispatch`. Actions sends only trigger identity and nonces; package bytes never leave the Durable Object for validation. Validator work uses a nonrenewable root-duration lease whose issue/fence times are derived from exact protected-ledger GitHub Dates and whose response object equals that attempt's journaled expected head; stale nonces and stale heads are fenced. One no-egress storage transaction reads the immutable sealed record, validates it, and atomically stores the canonical candidate, receipt, and independently encoded primary/fallback payloads. A pre-commit crash rolls back, and post-commit response loss reconciles from status without re-execution. The Git-capable relay receives immutable validated bytes only after atomic dispatch commit; persistent primary response failure uses the independent read-only digest-verifying fallback, with a retrieval-only zero-operation-storage-write repair as the final protected liveness path. Other post-dispatch repair remains Guardian-reviewed, operation-scoped, rollback-capable, and transport/provider-adapter-only. Unit 63a concretizes both disabled foundation plans before failure; a repair generation establishes and proves its selected runtime/storage/routes/bootstrap identity before operation-secret import or route activation, upgrades only that same foundation, and retains an executable reconciliation/rollback owner across every crash. Any repair broker and its complete foundation are removed or expiry-contained and proved unreachable before observation becomes effective. Delayed submission, crashed validation, retrieval failure, cancellation, broker failure, provider drift, and deadline therefore cannot strand or split a claim: unsubmitted bytes close, pre-dispatch bytes validate/retry/withdraw safely, a budget expiry during a live lease permits only status observation through terminal validation or exact fencing, and dispatched bytes resolve the frozen candidate. Shipment still requires effective `PublicationObserved` with terminal recovery cleanup.

Worker staging treats a version override as a capability-bearing selector, not authentication. A closed `WorkerOverrideBarrierV1` is established before candidate upload: production hostnames receive a provider-enforced default denial for the override header; one run-owned staging hostname is protected by a sole Access service-auth policy and a claim-bound broker-held provider token; and all service-binding callers strip or reject the header because Cloudflare forwards it when a caller forwards the original request. The provider token never leaves the broker; each staged invocation consumes a separate one-use operation authorization, while the token remains only for the compatibility window. The graph inventories rule/Access evaluation order and uses the correct private candidate version in hostile external/internal probes plus Worker observability. Failure removes candidate deployment membership before staging teardown; success promotes the same bytes to ordinary 100% traffic before revoking the staging provider token and exception. The permanent production-header denial remains after either path.

Photo Studio scenarios begin with a recipe lacking a cover and cover upload/generation behavior, preserve the original image in a Spoon where requested, verify editorialized-cover lifecycle and retry/offline behavior, inspect authoritative backend state, and clean only the run's identities and media. The exact scenario inventory will be validated against current source after native release ownership transfers.

## Progress Log

- 2026-07-20 19:58: Ari approved autonomous implementation and the recommended repository, parity, classification, and compatibility defaults.
- 2026-07-20 19:58: Created public repository `spoonjoy/spoonjoy-delivery`, repaired the failed automatic clone with an explicit HTTPS clone, and created isolated branch/worktree `worker/cross-client-delivery`.
- 2026-07-20 19:58: Created durable Desk task `spoonjoy/cross-client-delivery`, recorded ownership isolation from the active TestFlight task, and pushed Desk commit `32481dd28ee07ef1098c80c4bc4fd334d7f5dd54`.
- 2026-07-20 19:58: Drafted this plan after Tinfoil Hat and Stranger With Candy ideation scrutiny exposed stale narrative truth, missing compatibility phases, self-authored proof, native provenance gaps, and actor-proof conflation.
- 2026-07-20 20:12: Grounded the workflow design in the current repositories and replaced the impossible cross-repository secret inheritance assumption with source-owned dispatchable attestors, explicit credential-scope proof, exact GitHub run verification, and an append-only release-record definition.
- 2026-07-20 20:25: Addressed the first hostile plan review's three blockers and seven major findings: Candidate Authorization now gates provider mutations, compatibility uses staged-source then installed-production proof, digest identities are separated, rebaseline is mandatory, public artifacts are attested/redacted, cleanup is exact-manifest, and waiver/agent/hardware semantics fail closed.
- 2026-07-20 20:38: Addressed Round 2's three major findings by defining the immutable-actor single-operator authority policy, extending operation-scoped authorization to every provider mutation, and applying fail-closed evidence classification/redaction to screenshots, devices, queues, logs, cleanup, media, and provider artifacts.
- 2026-07-20 20:49: Addressed escalation review findings with an exact-run Execution Claim handshake, exhaustive compound-operation inventories and partial-failure receipts, and pre-stream stdout/stderr controls that prevent public Actions logs or caches from bypassing evidence classification.
- 2026-07-20 20:58: Closed the claim-publication race with one append-only authorization/claim ledger and expected-parent CAS, bound claims through deployment/job identity and environment approval, retained expired-claim exclusivity until containment, and replaced linear mutation lists with exact predicate-bound provider operation graphs.
- 2026-07-20 21:10: Made `ReleaseSetPublished` on the ledger the sole atomic shipment event, replaced self-declared actor identity with provider-bound protected workflow appends, constrained GitHub environment approval to one waiting mutation job, and added typed DAG dataflow with template/resolved request digests.
- 2026-07-20 21:20: Reserved authorization generations for active claims, made shipment the direct child of finalization claim, specified concrete GitHub environment reviewer/bypass/approval behavior, and separated static authorized DAG templates from hash-linked runtime execution receipts.
- 2026-07-20 21:27: Fresh convergence reviewer returned `CONVERGED` with no remaining BLOCKER or MAJOR findings; planning is approved for doing-doc conversion.
- 2026-07-21 00:59: Doing scrutiny refined finalization without changing product scope: durable abort-only recovery registration prevents stranded claims, independently supervised cleanup replaces self-attestation, and publication uses an honest bounded pre-CAS snapshot plus mandatory exact post-result observation rather than claiming unknowable provider age at Git ref CAS.
- 2026-07-21 01:26: Doing scrutiny hardened the architecture: receiver acknowledgments must live on both protected mains, one dedicated App installation is the ledger's only bypass principal, source teardown keys are attested and claim-bound before environment approval, native isolation is tested on macOS with UID-wide exhaustion, and final append/observation timing has executable bounds.
- 2026-07-21 02:00: Doing scrutiny replaced the remaining implicit mechanics with an executable root/genesis journal, exact `ProtectedMainV1`, real one-hour App-token revocation containment, broker-side actor/transition enforcement, pre-claim waiting final attestors, and a Durable Object prepare/CAS/observe mailbox protocol.
- 2026-07-21 02:25: Doing scrutiny made main check sources and ledger bypass principals exact, specified the root signature/code-manifest/journal transport, closed pre-mint crash containment, moved all Git object creation into the broker from bounded mailbox bytes, and restored complete time equations plus total CAS-uncertainty recovery.
- 2026-07-21 02:46: Tinfoil Round 9 separated credential-free pre-claim planning from the single post-approval provider snapshot, split append and token lifecycles, made every `E/F/H` timing/absence record executable, and specified reproducible root-manifest and P-256 byte domains.
- 2026-07-21 03:24: Tinfoil Round 10 made journal genesis and later appends concrete, added App/bootstrap and source-envelope key custody, introduced an independent post-PROMOTED witness, moved renewable queries before final-job freeze, and gave final evidence an executable end-to-end budget.
- 2026-07-21 03:52: Tinfoil Round 11 separated post-root governance upgrades from forbidden bootstrap replay, added provisional key challenge and exact HPKE, pre-dispatched the publication relay with a readiness barrier and complete teardown budget, added post-purge abort resolution, and removed public path-derived hashes.
- 2026-07-21 04:04: Exact `eae18d8` Tinfoil review returned clean; a post-review refinement then separated provisional challenge workflows from ordinary deploy/TestFlight preflights and ordered local key generation before the protected start event, requiring a new exact-head pass.
- 2026-07-21 10:55: Exact-head scrutiny remains open after Round 22 repairs: sealed packages now have signer-free replacement-relay recovery or atomic pre-dispatch withdrawal, failed Worker promotion is contained before downstream live work, finalization dependencies are literal, and tag projection has deterministic orphan-object and public-metadata privacy contracts.
- 2026-07-21 11:18: Round 23 repaired restartable root generations, complete Worker invocation-surface containment, and direct same-generation compatibility evidence dependencies; exact-head scrutiny remains open.
- 2026-07-21 11:55: Round 24 closed exact unstarted-genesis recovery, made submission close compete atomically with seal before any abort, replaced version-ID secrecy with a provider-enforced Access/WAF/service-binding Worker override barrier, and separated GitHub's commit-associated Release `created_at` from operation-time `published_at` without inventing an equality the live provider does not honor; exact-head scrutiny remains open.
- 2026-07-21 12:16: Round 25 repaired exact-head Tinfoil findings: slash-shorthand dependencies are gone, the complete doing document is its own positive lint fixture, and `ProductChangeReleasedWithWaiver` is a contained non-shipping Product Change terminal with an exact fresh-successor edge rather than a proof-node disposition.
- 2026-07-21 12:44: Round 26 repaired exact-head Tinfoil findings across finalization liveness, live-stage failure routing, replay cardinality, and waiver successor atomicity. The first relay now binds real bytes only at fetch, deterministic post-dispatch defects have a candidate-preserving reviewed recovery path, and exact typed terminals replace aggregate execution claims.
- 2026-07-21 13:55: Round 28 closed five exact-head Tinfoil findings: validation is now a no-egress Durable Object transaction with crash-safe lease reconciliation; replay has explicit failed/checkpointed/repair-completed states plus attempt recovery and exact repair terminals; two-phase readiness and claim expiry share one GitHub-Date domain; and repair publication remains ineffective until the operation broker is removed or expiry-contained and proved unreachable. Exact unit-reference lint also replaced the final repair-family shorthand.
- 2026-07-21 14:22: Round 29 closed three exact-head Tinfoil findings: root-bound GitHub-Date lease durations now bind every acquisition, renewal, and fence to the request's journaled expected ledger head; committed validation has an independent raw fallback and retrieval-only repair path; and every activated replay pass, including a zero-replay all-green pass, must consume the full manifest through Unit 60c59.
- 2026-07-21 14:44: Round 30 closed the fresh exact-head Tinfoil blocker and major finding: a sole root-bound atomic p1 genesis transition makes first replay activation reachable, and a complete original-to-executor identity bijection keeps all replay state in the original Unit-ID domain while failure blocks every remaining manifest node regardless disposition.
- 2026-07-21 15:09: Round 31 closed the fresh exact-head Tinfoil blocker by adding an original-ID manifest cursor: claims now serialize on one exact node with a consumed-prefix/unclaimed-suffix invariant, and containment consumes the causal node, blocks the complete suffix, fails the cursor, and fails the pass in one CAS with no successor-claim window.
- 2026-07-21 15:38: Round 32 closed three fresh exact-head findings: every live web/native/safety failure now reaches one generic protected repair checkpoint, registration-without-claim has an authoritative zero-claim cleanup and null-generation closure, and ordinary replay consumption has immutable receipts whose monotonic descendant chains survive later cursor progress and pass resolution.
- 2026-07-21 16:09: Round 33 repaired four fresh exact-head findings: Unit 36 now enters an explicit Unit 51 containment-pending bridge before causal failure; zero-registration and registered-zero-claim closures are distinct; an independent replacement relay owns exact-candidate append after first-dispatch death while recovery remains non-publishing; and `S/C/R` final query clocks now have exact authorities, unions, equations, and no-retry failure semantics.
- 2026-07-21 16:29: Tinfoil Round 34 froze candidate-specific staged-Worker cleanup authority for non-green Unit 32, Unit 33, Unit 34, and Unit 35 before generic suffix blocking, split provider mutation from credentialless terminal reconciliation so the mutator cannot attest its own teardown, and added a root-timed registration lease plus durable claim-dispatch intent and CAS-exclusive close-start so every registered-before-claim crash window has an executable owner.
- 2026-07-21 17:53: Tinfoil Round 35 moved staged cleanup onto each causal node's legal green-versus-cleanup CAS, made Unit 36 fence only from its own cursor, replaced broker self-teardown with permanent exact-purpose mutation/read services and run-scoped grants, and added a durable pre-registration dispatch intent plus sibling close-start CAS before destructive cleanup.
- 2026-07-21 18:16: Tinfoil Round 36 made registration intent an exclusive on-ledger reservation before dispatch so unrelated parent drift cannot strand both successors, and made replay Unit 36 fence only when the current pass retained a fresh available generation so all-no-replay passes remain traversable.
- 2026-07-21 18:56: Stranger With Candy Round 37 reset the clean sequence and required three-way delivery/web/native protected-main acknowledgment, resolver exclusion through registered-open and claim-intent-open heads, and forward-only web/native/safety failure checkpoints.
- 2026-07-21 19:26: Tinfoil Round 38 removed Unit 36 from checkpoint eligibility and generated one exact causal-ID table whose enforced non-green chain is `36 -> LiveStageContainmentPendingV1 -> 51 -> LiveStageFailedV1 -> 42a`.
- 2026-07-21 20:11: Stranger With Candy Round 39 reset the clean sequence and required explicit reviewed Worker/Durable Object broker foundations before every root, supervisor, cleanup, or ASC secret import, plus a generation-bound installed-queue recovery authority that survives Unit 34 green and terminally reconciles before every later live/replay failure, checkpoint, or fresh seed.
- 2026-07-21 20:34: Tinfoil Round 40 reset the clean sequence and extended foundation-before-activation to the two post-dispatch finalization repair lanes: root-reviewed retrieval-only and transport/provider-adapter templates, credential-free Unit 63a concrete plans, recovery-owned disabled foundations before secret/route/dispatch activation, same-foundation upgrade, operation-storage isolation, crash reconciliation, and complete teardown before effective observation.
- 2026-07-21 21:12: Tinfoil Round 41 reset the clean sequence and closed the pre-registration liveness gap with an attempt-specific recovery authority that survives coordinator death through supervisor-mediated cleanup, signed receipt reconciliation, exact closed append, complete nonreachability, and ordered teardown while preserving separation between cleanup and ledger authority.
